import { Card } from "@heroui/react";

interface  PropTypes {
  pageName: string
}

export default function NavTile({pageName} : PropTypes) {
  return (
    <>
      <p >Flex grid test content</p>
    </>
  )
}